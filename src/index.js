import React from 'react';
import ReactDOM from 'react-dom';
import LivepeerStreaming from './LivepeerStreaming';

ReactDOM.render(
  <React.StrictMode>
    <LivepeerStreaming />
  </React.StrictMode>,
  document.getElementById('root')
);